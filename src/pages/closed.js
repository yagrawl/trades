import React, { Component } from 'react';

import ClosedContainer from '../containers/closed';

class Closed extends Component {
  render() {
    return (
      <div className="page-main-div">
        <ClosedContainer />
      </div>
    );
  }
}

export default Closed;
